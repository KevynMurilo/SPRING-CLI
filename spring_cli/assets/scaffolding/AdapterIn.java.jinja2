package {{ package_name }}.adapters.in.web;

import {{ package_name }}.ports.in.{{ entity_name }}InputPort;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/{{ entity_name | lower }}")
public class {{ entity_name }}Controller {

    private final {{ entity_name }}InputPort inputPort;

    public {{ entity_name }}Controller({{ entity_name }}InputPort inputPort) {
        this.inputPort = inputPort;
    }

    @GetMapping
    public String get() {
        return inputPort.execute();
    }
}
